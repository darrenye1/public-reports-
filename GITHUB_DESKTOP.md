# ? GitHub Desktop ??????? Vercel

## ??????????? GitHub??????

### ? GitHub ????? `public-reports-`

1. ?? **GitHub Desktop**
2. **File ? Add local repository**
3. ?????`f:\??\Equity research report`
4. ??? *not a Git repository* ? ? **create a repository**
   - Name?????? `equity-research`
   - ???? *Initialize with README*?????????
5. **Repository ? Repository settings ? Remote**
   - ??? **Publish repository** / **Push origin**
6. ? **Publish to GitHub** ? **Add remote**?
   - URL?`https://github.com/darrenye1/public-reports-.git`??????????
   - ???`main`
7. ??????
   - ?????????????**??**?? `.github`?`public`?`equity_research.py` ??
   - Summary ??`Initial commit`
   - ? **Commit to main** ? **Push origin**

### ? GitHub ??????????????

? **Pull** ? **Push**??????

1. **Repository ? Pull**
2. ???????? `public/` ????????????

---

## Vercel ????????

1. [vercel.com](https://vercel.com) ? ?? ? **Settings ? Git**
2. ?? **???** GitHub ??
3. **Output Directory** = `public`?Build Command ??

???? **Push** ? `main`?Vercel ? 1–2 ?????????

---

## ??????? / PDF / ????????

### 1. ??????

???

```bat
run_daily_refresh.bat
```

???? Python ? `sync_website.bat`??? `public/`?

??**????**?

```powershell
cd "f:\??\Equity research report"
python equity_research.py --batch-top20 --force --replace-old
sync_website.bat
```

### 2. ? Desktop ?? GitHub

1. ?? GitHub Desktop??????
2. ????????????
   - `public/summary-data.js`
   - `public/reports/*.pdf`?`Top20_Summary.csv`
3. ?? **Summary** ??`Update Top20 and reports`
4. **Commit to main**
5. ? **Push origin**

### 3. ????

- Vercel ? **Deployments** ? ??? **Ready**
- ?? https://public-reports-one.vercel.app/ ? ?? **OK (20)**

---

## ???????

? `public/index.html`?? CSS?? Desktop ?? commit `public/` ? **Push**???? Python?

---

## ??????????? Desktop?

????? `.github/workflows/update-website.yml` ??

1. GitHub ?? ? **Settings ? Actions** ? ? **Read and write**
2. **Actions** ? **Update research and website** ? **Run workflow**

???????? push?Vercel ??????????????? Push ?????? Desktop ?????

---

## ????

**Push ??? / ??**  
? **Repository ? Pull**??????? Push?

**Vercel ???**  
? ?? Push ???? Vercel ??????? `main` ???

**Desktop ??????????**  
? ???? `public/` ???????????? `summary-data.js` ? `public/reports/`?
